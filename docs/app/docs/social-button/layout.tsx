import type { Metadata } from "next";
export const metadata: Metadata = { title: "Social Button", description: "AniUI Social Button — Branded social sign-in buttons for OAuth flows.", alternates: { canonical: "/docs/social-button" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
