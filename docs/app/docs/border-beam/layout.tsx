import type { Metadata } from "next";
export const metadata: Metadata = { title: "Border Beam", description: "AniUI Border Beam — Skia sweep-gradient beam tracing a rounded border.", alternates: { canonical: "/docs/border-beam" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
