import type { Metadata } from "next";
export const metadata: Metadata = { title: "Mobile Dock", description: "AniUI Mobile Dock — macOS-style dock strip with scale and lift on focus.", alternates: { canonical: "/docs/mobile-dock" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
