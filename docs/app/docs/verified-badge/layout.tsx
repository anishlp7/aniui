import type { Metadata } from "next";
export const metadata: Metadata = { title: "Verified Badge", description: "AniUI Verified Badge — Verified check badge with optional pulse shimmer.", alternates: { canonical: "/docs/verified-badge" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
