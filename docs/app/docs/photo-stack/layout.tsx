import type { Metadata } from "next";
export const metadata: Metadata = { title: "Photo Stack", description: "AniUI Photo Stack — Overlapping stacked photos with rotation offsets.", alternates: { canonical: "/docs/photo-stack" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
