import type { Metadata } from "next";
export const metadata: Metadata = { title: "Roadmap", description: "AniUI component roadmap — motion, UI pieces, and Skia effects.", alternates: { canonical: "/docs/roadmap" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
