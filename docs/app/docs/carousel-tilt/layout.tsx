import type { Metadata } from "next";
export const metadata: Metadata = { title: "Carousel Tilt", description: "AniUI Carousel Tilt — Horizontal carousel with perspective tilt on off-center slides.", alternates: { canonical: "/docs/carousel-tilt" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
