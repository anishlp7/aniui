import type { Metadata } from "next";
export const metadata: Metadata = { title: "Rolling Counter", description: "AniUI Rolling Counter — Animated odometer-style rolling number counter.", alternates: { canonical: "/docs/rolling-counter" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
