import type { Metadata } from "next";
export const metadata: Metadata = { title: "Morphing Tab Bar", description: "AniUI Morphing Tab Bar — Tab bar with spring-animated morphing pill indicator.", alternates: { canonical: "/docs/morphing-tabbar" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
