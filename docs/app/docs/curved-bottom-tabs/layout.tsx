import type { Metadata } from "next";
export const metadata: Metadata = { title: "Curved Bottom Tabs", description: "AniUI Curved Bottom Tabs — Bottom tab bar with curved notch and elevated center action.", alternates: { canonical: "/docs/curved-bottom-tabs" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
