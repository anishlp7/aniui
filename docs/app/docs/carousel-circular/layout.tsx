import type { Metadata } from "next";
export const metadata: Metadata = { title: "Carousel Circular", description: "AniUI Carousel Circular — Circular orbit carousel — pan to rotate items around a ring.", alternates: { canonical: "/docs/carousel-circular" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
