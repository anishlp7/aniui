import type { Metadata } from "next";
export const metadata: Metadata = { title: "Radiant Button", description: "AniUI Radiant Button — CTA button with pulsing Skia radial glow.", alternates: { canonical: "/docs/radiant-button" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
