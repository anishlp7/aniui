import type { Metadata } from "next";
export const metadata: Metadata = { title: "Carousel Scale", description: "AniUI Carousel Scale — Center-focused carousel with dramatic scale on the active slide.", alternates: { canonical: "/docs/carousel-scale" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
