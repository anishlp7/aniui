import type { Metadata } from "next";
export const metadata: Metadata = { title: "Verified Shine", description: "AniUI Verified Shine — Verified badge with Skia shine sweep.", alternates: { canonical: "/docs/verified-shine" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
