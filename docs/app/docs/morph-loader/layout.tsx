import type { Metadata } from "next";
export const metadata: Metadata = { title: "Morph Loader", description: "AniUI Morph Loader — Skia morphing rounded-square loader.", alternates: { canonical: "/docs/morph-loader" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
