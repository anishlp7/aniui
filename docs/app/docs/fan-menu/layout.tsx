import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fan Menu", description: "AniUI Fan Menu — Radial fan menu expanding actions in an arc from a FAB.", alternates: { canonical: "/docs/fan-menu" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
