import type { Metadata } from "next";
export const metadata: Metadata = { title: "Barcode Badge", description: "AniUI Barcode Badge — Decorative barcode strip with monospace label.", alternates: { canonical: "/docs/barcode-badge" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
